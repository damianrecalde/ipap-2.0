{% extends 'base.html.twig' %}

{% block title %}WorkTeam{% endblock %}

{% block body %}
    <h1>WorkTeam</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work_team.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ work_team.name }}</td>
            </tr>
            <tr>
                <th>Createat</th>
                <td>{{ work_team.createat ? work_team.createat|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updateat</th>
                <td>{{ work_team.updateat ? work_team.updateat|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_work_team_index') }}">back to list</a>

    <a href="{{ path('app_work_team_edit', {'id': work_team.id}) }}">edit</a>

    {{ include('work_team/_delete_form.html.twig') }}
{% endblock %}
