<form method="post" action="{{ path('app_work_team_delete', {'id': work_team.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ work_team.id) }}">
    <button class="btn">Delete</button>
</form>
